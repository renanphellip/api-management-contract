name: '2. Develop'

on:
  pull_request:
    types:
      - closed
    branches:
      - develop

jobs:
  develop:
    name: Develop
    uses: renanphellip/api-management-nested-workflows/.github/workflows/develop.yml@main
