on:
  pull_request:
    types:
      - opened
      - edited
      - reopened
      - synchronize
    branches:
      - 'release/v*'

jobs:
  gates:
    uses: renanphellip/api-management-nested-workflows/.github/workflows/core-gates.yml@main
